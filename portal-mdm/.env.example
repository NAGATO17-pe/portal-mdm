# --- FastAPI backend ---
NEXT_PUBLIC_API_URL=http://localhost:8000

# --- Auth ---
JWT_COOKIE_NAME=mdm_session
# Shared secret used by FastAPI to sign JWTs. Required in production for
# signature verification in lib/auth/session.ts. Leave empty in local dev to
# fall back to optimistic decode.
JWT_PUBLIC_SECRET=
