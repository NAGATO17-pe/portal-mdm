import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function EntitiesPage() {
  return (
    <PagePlaceholder
      title="Entidades MDM"
      description="Listado por tipo de entidad con filtros, paginación server-side y bulk actions."
      sprint="Sprint 2 · Módulo Admin"
    />
  );
}
